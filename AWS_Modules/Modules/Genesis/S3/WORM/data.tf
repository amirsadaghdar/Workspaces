# Data
/* data "template_file" "state_policy_file" {
  template = file("${path.module}/policy.json")

  vars = {
    bucket_name     = var.aws_bucket_name
    account_id      = var.aws_account_id
    netapp_username = var.netapp_username
  }
} */
